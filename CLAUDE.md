Read AGENTS.md before installing, verifying or modifying anything here. It
carries the audit checklist (expected network egress, where secrets flow),
the agent install path, and the behaviors that look like bugs but are not.
